Place these files here:

- `custom_cnn_best.pt`
- `label_mapping.json`

Do not commit large model checkpoints to a normal Git repository. Use Git LFS, a model registry, or release artifact storage for production.
